{
  "strmrRequestTemplate": {
    "application/json": {
      "movie": "$input.params('movie')",
      "uid": "$input.params('uid')",
      "u": "$input.params('u')",
      "p": "$input.params('p')"
    }
  }
}
